export interface IMenuUser{
    id: number;
    name: string;
}

export interface IGradient{
    id: number;
    color: string;
}

export interface IBoard{
    title: string,
    color: string,
    animation: boolean
}